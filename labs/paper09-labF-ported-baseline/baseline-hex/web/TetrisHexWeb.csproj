<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>Tetris.HexWeb</RootNamespace>
    <AssemblyName>TetrisHexWeb</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <!-- Staging 2. A WebSocket driving adapter (socket message -> driving-port
         call) plus one new driven adapter (WebSocketBoardOutput). The hexagon and
         the shared piece-selection adapter are taken as they are. -->
    <ProjectReference Include="..\domain\TetrisHexDomain.csproj" />
    <ProjectReference Include="..\adapters\TetrisHexAdapters.csproj" />
  </ItemGroup>

</Project>
