<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>Tetris.HexWebRest</RootNamespace>
    <AssemblyName>TetrisHexWebRest</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <!-- Staging 3. An HTTP driving adapter (POST -> driving-port call) plus one
         new driven adapter (SseBoardOutput). The hexagon is taken as it is; the
         wire renderer now comes from ..\adapters, which this staging forced out
         of staging 2. -->
    <ProjectReference Include="..\domain\TetrisHexDomain.csproj" />
    <ProjectReference Include="..\adapters\TetrisHexAdapters.csproj" />
  </ItemGroup>

</Project>
