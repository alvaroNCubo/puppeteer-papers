<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net9.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>Tetris.Input</RootNamespace>
    <AssemblyName>TetrisStage</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <!-- Drives the warm TetrisActor (Check-guarded verbs + RunReactions + the Job
         reaction/push) and reuses FrameFileSink, BoardRenderer, SessionPaths. -->
    <ProjectReference Include="..\actor\TetrisActor.csproj" />
  </ItemGroup>

</Project>
