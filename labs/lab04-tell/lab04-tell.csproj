<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net9.0</TargetFramework>
    <LangVersion>latest</LangVersion>
    <ImplicitUsings>disable</ImplicitUsings>
    <Nullable>disable</Nullable>
    <IsPackable>false</IsPackable>
    <AssemblyName>Lab04Tell</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <!-- Sibling clone of https://github.com/alvaroNCubo/puppeteer at the cited
         provenance commit (see README) — same convention as lab01/lab02/lab03.
         Choreography supplies the broker transport for the separated-receiver run (G5). -->
    <ProjectReference Include="..\..\..\puppeteer\Puppeteer\Puppeteer.csproj" />
    <ProjectReference Include="..\..\..\puppeteer\Choreography\Choreography.csproj" />
  </ItemGroup>

</Project>
